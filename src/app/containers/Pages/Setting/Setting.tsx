import * as React from "react";
import CardBody from "../../../components/UI/Card/CardBody";

interface SettingProps {

}

const Setting: React.FC<SettingProps> = (props: any) => {

    return (
        <CardBody title="Setting" >
        </CardBody>
    );
}

export default Setting;
