import * as React from "react";
import CardBody from "../../../components/UI/Card/CardBody";

interface LightingMapProps {

}

const LightingMap: React.FC<LightingMapProps> = (props: LightingMapProps) => {

    return (
        <CardBody title="Map" >
        </CardBody>
    );
}

export default LightingMap;
